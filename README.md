# fixed_IDOR_example_SuperTets-Express
A simple real-life IDOR example front-end login page with a fixed vulnerable backend written in node.js using express.js framework.  For testing the supertest is used
